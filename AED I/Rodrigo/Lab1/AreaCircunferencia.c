# include <stdio.h>
# include <stdlib.h>

int main(){
    int raio;
    float pi, area;
    raio = 5;
    pi = 3.14159;
    area = pi * (raio * raio);
    printf("A area da circunferencia = %.1f \n", area);
    return 0;
}